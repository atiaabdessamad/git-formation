---
platform: linux

image_resource:
  type: docker-image
  source: { repository: starkandwayne/concourse }

inputs:
- name: resource-git-formation
outputs:
- name: updated
run:
  path: bash
  args: [publish.sh]
  dir: resource-git-formation