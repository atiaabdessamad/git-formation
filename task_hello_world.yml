---
platform: linux

image_resource:
  type: docker-image
  source: {repository: busybox}

inputs:
- name: resource-git-formation

run:
  path: ls

run:
  path: cat
  args: [resource-git-formation/README.md]
